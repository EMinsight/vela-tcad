# PN2D BV atomic default decision

Date: 2026-08-01

Decision: **authorized for the restricted PN2D BV template surface**.

The prospective machine acceptance passed, the independent scientific review
returned `APPROVE`, and the final independent code review returned `APPROVE`.
The PN2D BV template version 3 may therefore default to the atomic
SG/Laux + mixed-Voronoi + non-obtuse-qualified profile defined by the frozen
v2 contract.

The authorization does not extend to global C++ defaults, PN2D IV, arbitrary
obtuse/non-Delaunay meshes, or other device/material/bias domains. The named
legacy profile remains the complete barycentric rollback path.

Machine-readable decision:
`docs/validation/pn2d_node_volume_policy_atomic_default_decision_2026-08-01.json`.
